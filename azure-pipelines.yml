trigger:
- main

pool:
  Default

steps:
- bash: az --version
  displayName: 'Show Azure CLI version'

- bash: az vm list -d | python toggle.py
  displayName: 'Toggling from Python'

- bash: sleep 120
  displayName: 'Wait for 2 minutes'

- bash: az vm list -d | python toggle.py
  displayName: 'Toggling from Python'
